spring.application.name=yaAllah
# Use prod profile for Neon database
spring.profiles.active=prod

server.port=${PORT:8080}

# PostgreSQL - Neon Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-steep-salad-addjdimb-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_J9CvMslXj3px}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.show-sql=true
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.open-in-view=false

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Prefer JDK proxies to avoid CGLIB proxying final methods (Filter warnings)
spring.aop.proxy-target-class=false

jwt.secret=YourSuperSecretJwtKeyThatIsAtLeast32BytesLongForHS256Algorithm
jwt.expiration-ms=86400000

# CORS: comma-separated list of allowed origins.
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,https://*.vercel.app}

springdoc.swagger-ui.path=/swagger-ui.html

spring.security.user.name=admin
spring.security.user.password=admin123

# Admin User Credentials
app.admin.name=admin
app.admin.email=admin@example.com
app.admin.password=admin
app.admin.phoneNumber=1234567890

# Backend base URL used to build absolute image URLs in production.
# On Render, set this env var to your backend public URL, e.g. https://backend-7mzg.onrender.com
app.backend-base-url=${BACKEND_BASE_URL:}

# Cloudinary Configuration
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dextn3d5a}
cloudinary.api-key=${CLOUDINARY_API_KEY:137336551494142}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:-c5OkTR1FZoXovvzIDvoeYAf7kc}
