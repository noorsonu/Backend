spring.application.name=yaAllah

server.port=${PORT:8080}

# PostgreSQL - Environment Variables for Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-red-unit-adggs0c8-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_sLgV54JkEYWO}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Connection Pool - Render Optimized
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# Security
jwt.secret=${JWT_SECRET:YaAllahSuperSecretJwtKeyForProductionUse2024}
jwt.expiration-ms=86400000

# CORS
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173,https://*.vercel.app,https://*.onrender.com}

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html

# Admin
app.admin.name=${ADMIN_NAME:admin}
app.admin.email=${ADMIN_EMAIL:admin@example.com}
app.admin.password=${ADMIN_PASSWORD:admin}
app.admin.phoneNumber=${ADMIN_PHONE:1234567890}

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dextn3d5a}
cloudinary.api-key=${CLOUDINARY_API_KEY:137336551494142}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:-c5OkTR1FZoXovvzIDvoeYAf7kc}